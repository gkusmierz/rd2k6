Uruchom agenta PREREQ-CHECK.

Przeczytaj plik `.claude/agents/PREREQ-CHECK.md` i wykonaj wszystkie kroki opisane w tym agencie.

Parametr wejściowy: $ARGUMENTS (ścieżka do root projektu C++/Qt do analizy).

Jeśli $ARGUMENTS jest pusty, użyj bieżącego katalogu roboczego (`.`) jako root projektu — zakładamy, że system został skopiowany do analizowanego projektu.
