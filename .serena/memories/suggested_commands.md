# Suggested Commands

## Build
```bash
./autogen.sh
./configure
make -j$(nproc)
```

## System utilities
- git, ls, cd, grep, find — standard Linux commands
- bear -- make — regenerate compile_commands.json
